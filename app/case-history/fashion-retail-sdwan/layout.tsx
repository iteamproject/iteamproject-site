import { legacyMetadata } from "@/lib/seo";

export const metadata = legacyMetadata.fashionRetailSdwan;

export default function CaseHistoryLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
